# Server port
PORT=3001

# Maximum concurrent PTY sessions
MAX_SESSIONS=10

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3001

# SQLite database path (auto-created if missing)
DB_PATH=./data/agent-deck.db
